module TypeResolutionSpec where
import Base
{- import TypeResolution -}
{- import Parser -}
import Test.Hspec

run :: Spec
run = return ()
  {- describe "assigns types to variables" $ do -}
    {- it "" $ do -}
      {- let foo = Function "foo" [Integer] (Integer) [Var "a"] (Digit 2)  -}
      {- let assignment = Assignment (Var "a") (Call "foo" []) -}
      {- let ast = [foo, assignment] -}
      {- resolve ast `shouldBe`   -}
